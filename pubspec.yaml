name: fashion_store
description: A luxury fashion store mobile application
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.1.1
  firebase_auth: ^5.1.1
  provider: ^6.1.1
  google_fonts: ^6.2.1
  go_router: ^14.2.0
  cached_network_image: ^3.3.1
  shared_preferences: ^2.2.2
  cloud_firestore: ^5.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true